"use strict";
/**
 * {{name}}
 * @type {XpresserControllerServices|any}
 */
const {{name}} = {};
{{name}}.namespace = null;

{{name}}.exampleService = (options, {error}) => {
    // Logic Here or return error
};

module.exports = {{name}};
