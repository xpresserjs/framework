/**
 * {{name}}
 * @type {Xpresser.Controller.Service|*}
 */
const {{name}} = {};
{{name}}.namespace = null;

{{name}}.exampleService = (options, {error}) => {
    // Logic Here or return error
};

module.exports = {{name}};
