/**
 * {{name}}
 */
module.exports = {
    namespace: "{{namespace}}",

    /**
     * The `index` method is called by default.
     * it also inherits namespace as name.
     */
    index() {
        // Your Code
    }
};
