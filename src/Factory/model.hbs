/**
* {{name}} Model
* @class
* @extends $.model
*/
class {{name}} extends $.model {

    /**
    * Name of model database table name.
    * @method tableName
    * @returns {string}
    */
    static get tableName() {
        return "{{table}}";
    }

}

{{name}}.prototype.$hidden = [];

module.exports = {{name}};
