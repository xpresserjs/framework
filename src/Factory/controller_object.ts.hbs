import {Controller, Http} from "xpresser/types/http";

/**
 * {{name}}
 */
export = <Controller.Object>{
    // Controller Name
    name: "{{name}}",

    // Controller Default Error Handler.
    e: (http: Http, error: string) => http.send({ error }),


    /**
    * Example Action.
    * @param http - Current Http Instance
    */
    action(http) {
        return http.send({
            route: http.route
        });
    }
};
